# Universal AI Assistant - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │         UniversalAssistant.tsx (Floating Window)          │ │
│  │                                                            │ │
│  │  • Context Indicator (🌐💻📝)                             │ │
│  │  • Message History                                         │ │
│  │  • Input Field                                             │ │
│  │  • Settings Button                                         │ │
│  └────────────────────────┬──────────────────────────────────┘ │
│                           │                                     │
│  ┌────────────────────────▼──────────────────────────────────┐ │
│  │              AIConfig.tsx (Settings Modal)                │ │
│  │                                                            │ │
│  │  • Provider Status                                         │ │
│  │  • API Key Management                                      │ │
│  │  • Connection Testing                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ invoke/listen
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND SERVICES LAYER                      │
│                                                                 │
│  ┌────────────────────┐      ┌────────────────────────────┐   │
│  │ Active Window      │      │   Context-Aware Routing    │   │
│  │ Context Service    │◄────►│        Service             │   │
│  │                    │      │                            │   │
│  │ • Platform detect  │      │ • 12 routing rules         │   │
│  │ • 13 app types     │      │ • Content analysis         │   │
│  │ • 100ms monitoring │      │ • Template selection       │   │
│  │ • Privacy controls │      │ • Confidence scoring       │   │
│  └────────┬───────────┘      └─────────────┬──────────────┘   │
│           │                                 │                  │
│           └────────────┬────────────────────┘                  │
│                        │                                       │
│           ┌────────────▼───────────────────────┐              │
│           │   Universal AI Service             │              │
│           │                                     │              │
│           │ • Unified provider interface       │              │
│           │ • Automatic routing                │              │
│           │ • Fallback chains                  │              │
│           │ • Request orchestration            │              │
│           └────────┬───────────────────────────┘              │
│                    │                                           │
│      ┌─────────────┼─────────────┬──────────────┐            │
│      │             │             │              │             │
│  ┌───▼────┐   ┌───▼────┐   ┌───▼─────┐   ┌───▼──────┐      │
│  │Enhanced│   │Priority│   │OpenAI   │   │Custom    │       │
│  │Prompt  │   │Strategy│   │Client   │   │Model     │       │
│  │Service │   │Service │   │Service  │   │Service   │       │
│  └────────┘   └────────┘   └─────┬───┘   └──────────┘       │
│                                   │                           │
└───────────────────────────────────┼───────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │               │               │
            ┌───────▼──────┐  ┌────▼─────┐  ┌─────▼─────┐
            │              │  │          │  │           │
            │   Ollama     │  │ OpenAI   │  │Perplexity │
            │   (Local)    │  │ (Cloud)  │  │ (Cloud)   │
            │              │  │          │  │           │
            │ localhost:   │  │ api.     │  │ api.      │
            │   11434      │  │ openai   │  │ perplexity│
            └──────────────┘  └──────────┘  └───────────┘
                    ▲
                    │
        ┌───────────┴────────────┐
        │                        │
┌───────▼─────────┐     ┌───────▼─────────┐
│ Ollama Manager  │     │  Model Registry │
│   Service       │     │    Service      │
│                 │     │                 │
│ • Install check │     │ • 6+ models     │
│ • Model list    │     │ • Download mgmt │
│ • Version check │     │ • Metadata      │
└─────────────────┘     └─────────────────┘
                                    │
                                    │
┌───────────────────────────────────▼───────────────────────────┐
│                    BACKEND (Tauri/Rust)                       │
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Context Detection Module                     │ │
│  │                                                            │ │
│  │  Commands:                                                 │ │
│  │  • get_active_window_info()                               │ │
│  │  • get_browser_context()                                  │ │
│  │  • get_editor_context()                                   │ │
│  │  • get_office_context()                                   │ │
│  │  • get_file_explorer_context()                            │ │
│  │  • get_terminal_context()                                 │ │
│  │  • get_selected_text()                                    │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  windows.rs  │  │   macos.rs   │  │   linux.rs   │        │
│  │              │  │              │  │              │        │
│  │  Win32 API   │  │  NSWorkspace │  │    X11 API   │        │
│  │  • GetFore…  │  │  • frontmost │  │  • _NET_…    │        │
│  │  • GetWin…   │  │  • AXUIElem  │  │  • WM_CLASS  │        │
│  │  • GetProc…  │  │  • AppleScr  │  │  • PRIMARY   │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    OPERATING SYSTEM                             │
│                                                                 │
│  • Active Window Management                                     │
│  • Process Information                                          │
│  • Accessibility APIs                                           │
│  • Clipboard/Selection                                          │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagrams

### 1. Query Processing Flow

```
┌──────────────┐
│ User Types   │
│ Query        │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│ UniversalAssistant.tsx               │
│ • Capture input                      │
│ • Add to message history (user)      │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Universal AI Service                 │
│ • Get current context                │
│ • Determine template                 │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Context-Aware Routing                │
│ • Analyze app type                   │
│ • Detect content (math/code/academic)│
│ • Apply priority rules               │
│ • Return: { template, confidence }   │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Enhanced Prompt Service              │
│ • Load template                      │
│ • Inject variables                   │
│ • Add conversation history           │
│ • Return: { systemPrompt, userPrompt}│
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Priority Strategy Service            │
│ • Map template → task type           │
│ • Check provider availability        │
│ • Select optimal provider & model    │
│ • Build fallback chain               │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Send to AI Provider                  │
│ • Ollama (local) OR                  │
│ • OpenAI (cloud) OR                  │
│ • Custom model                       │
└──────┬───────────────────────────────┘
       │
       ▼ (on success)
┌──────────────────────────────────────┐
│ Process Response                     │
│ • Extract content                    │
│ • Add to message history (assistant) │
│ • Update UI                          │
│ • Display to user                    │
└──────────────────────────────────────┘
       │
       ▼ (on failure)
┌──────────────────────────────────────┐
│ Try Fallback Provider                │
│ • local → openai → custom            │
│ • Retry with next in chain           │
└──────────────────────────────────────┘
```

### 2. Context Detection Flow

```
┌──────────────┐
│ Timer Tick   │
│ (every 100ms)│
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Active Window Context Service        │
│ • detectContext()                    │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Invoke Backend Command               │
│ invoke('get_active_window_info')     │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Platform-Specific Detection          │
│ Windows: GetForegroundWindow()       │
│ macOS:   NSWorkspace.frontmost       │
│ Linux:   X11 _NET_ACTIVE_WINDOW      │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Extract Context Data                 │
│ • Process name                       │
│ • Window title                       │
│ • Executable path                    │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Classify Application                 │
│ • Browser? Code editor? Office?      │
│ • Match against known apps           │
│ • Assign ApplicationType             │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Extract App-Specific Context         │
│ Browser:    URL, page title          │
│ Code:       File path, language      │
│ Office:     Document name            │
│ Terminal:   Current directory        │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Capture Selected Text (optional)     │
│ invoke('get_selected_text')          │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Build ApplicationContext Object      │
│ {                                    │
│   type, name, executable,            │
│   windowTitle, context,              │
│   selectedText, timestamp,           │
│   confidence                         │
│ }                                    │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Notify Listeners                     │
│ • UniversalAssistant updates UI      │
│ • Context indicator refreshes        │
└──────────────────────────────────────┘
```

### 3. Provider Selection Flow

```
┌──────────────┐
│ AI Request   │
│ Received     │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Check: Force Provider?               │
│ request.forceProvider                │
└──────┬──────────┬────────────────────┘
       │ YES      │ NO
       │          │
       ▼          ▼
┌──────────┐  ┌──────────────────────┐
│ Use      │  │ Priority Strategy    │
│ Forced   │  │ • Map template→task  │
│ Provider │  │ • Check local-first  │
└──────┬───┘  │ • Network status     │
       │      │ • Provider health    │
       │      └──────┬───────────────┘
       │             │
       └─────┬───────┘
             │
             ▼
┌──────────────────────────────────────┐
│ Get Applicable Providers             │
│ • Check if running/configured        │
│ • Check confidence threshold         │
│ • Check network requirement          │
│ • Filter & sort by priority          │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Selected Provider Chain              │
│ Primary:  local (ollama)             │
│ Fallback: openai                     │
│ Fallback: custom                     │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Select Model for Provider            │
│ • Ollama: llama2, mistral, codellama │
│ • OpenAI: gpt-3.5-turbo, gpt-4       │
│ • Custom: user-imported models       │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Return SelectionResult               │
│ {                                    │
│   provider, model, reason,           │
│   fallbackChain, isLocal,            │
│   estimatedLatency                   │
│ }                                    │
└──────────────────────────────────────┘
```

## 🧩 Component Dependencies

```
UniversalAssistant.tsx
├── → activeWindowContext (service)
├── → contextAwareRouting (service)
├── → universalAI (service)
└── → AIConfig.tsx (component)

AIConfig.tsx
└── → universalAI (service)

universalAI (service)
├── → contextAwareRouting (service)
├── → enhancedPromptService (service)
├── → priorityStrategy (service)
├── → openaiClient (service)
└── → ollamaManager (service)

contextAwareRouting (service)
└── → enhancedPromptService (service)

priorityStrategy (service)
├── → ollamaManager (service)
├── → modelRegistry (service)
└── → customModelService (service)
```

## 📦 Service Responsibilities

| Service | Responsibility | Exports |
|---------|---------------|---------|
| **activeWindowContext** | Detect active app, extract context | `startMonitoring()`, `getCurrentContext()`, `addListener()` |
| **contextAwareRouting** | Route to optimal template | `route()`, `generateContextAwarePrompt()`, `addRule()` |
| **universalAI** | Unified AI interface | `sendRequest()`, `sendStreamingRequest()`, `getProviderStatus()` |
| **enhancedPromptService** | Template management | `generatePrompt()`, `getTemplate()`, `recommendTemplate()` |
| **priorityStrategy** | Provider selection | `selectProvider()`, `checkNetworkStatus()`, `recordSuccess()` |
| **ollamaManager** | Ollama integration | `isOllamaRunning()`, `listModels()`, `oneClickInstall()` |
| **openaiClient** | OpenAI API | `sendRequest()`, `testConnection()`, `setRateLimitDelay()` |
| **modelRegistry** | Model catalog | `getAllModels()`, `downloadModel()`, `selectInstallPath()` |
| **customModelService** | Custom models | `importModel()`, `validateModelFile()`, `browseForModel()` |

## 🔐 Data Flow - Privacy & Security

```
┌──────────────────────────────────────┐
│ User's Active Window                 │
│ (Browser, VS Code, Word, etc.)       │
└──────┬───────────────────────────────┘
       │
       ▼ Platform APIs (Local)
┌──────────────────────────────────────┐
│ Backend Context Detection            │
│ • Runs in Rust (sandboxed)           │
│ • No data leaves machine             │
│ • Respects privacy settings          │
└──────┬───────────────────────────────┘
       │
       ▼ IPC (In-Process)
┌──────────────────────────────────────┐
│ Frontend Services                    │
│ • Process context locally            │
│ • No external calls yet              │
└──────┬───────────────────────────────┘
       │
       ▼ User Query Submitted
┌──────────────────────────────────────┐
│ Provider Selection                   │
│ • LOCAL (Ollama) = 100% private      │
│ • CLOUD (OpenAI) = data sent         │
└──────┬─────────┬─────────────────────┘
       │ LOCAL   │ CLOUD
       │         │
       ▼         ▼
┌──────────┐  ┌─────────────┐
│ Ollama   │  │ OpenAI API  │
│ Local    │  │ (Internet)  │
│ Machine  │  │             │
│ Private  │  │ See their   │
│ 🔒       │  │ policy      │
└──────────┘  └─────────────┘
```

**Key Privacy Features:**
- ✅ Context detection is 100% local
- ✅ No data sent without user query
- ✅ Local AI option (complete privacy)
- ✅ Configurable app blacklist
- ✅ Privacy mode disables sensitive capture
- ⚠️ Cloud providers: user's responsibility

---

**Created**: October 25, 2025  
**For**: Ask OCR Universal AI Assistant  
**Version**: 1.0
